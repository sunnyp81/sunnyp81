# Paperclip Use Cases — Weekly Scan Log

Append-only log. Newest entries on top. Format: `date | source | use case | url | applicability to Sunny's stack`.

Scan protocol: see `scan-protocol.md`. Skill auto-reads this when the user asks "what's new in Paperclip" or when designing a new agent/company.

---

