# Hermes data pullers — daily snapshot at 04:15 UTC.
# Install on the VPS:
#   crontab -l | grep -v hermes_ > /tmp/ct && cat hermes/deploy/hermes-crontab.txt >> /tmp/ct && crontab /tmp/ct
#
# Adjust the paths (HERMES_HOME, PYTHON) to your VPS layout.

HERMES_HOME=/root/.hermes
PYTHON=/root/.hermes/.venv/bin/python
BING_API_KEY=fd0147cf4f4446f4984568ee673533e6

# GSC — daily at 04:15 UTC
15 4 * * *  cd $HERMES_HOME && $PYTHON pullers/gsc_pull.py  >> logs/hermes_gsc.log  2>&1

# GA4 — daily at 04:30 UTC
30 4 * * *  cd $HERMES_HOME && $PYTHON pullers/ga4_pull.py  >> logs/hermes_ga4.log  2>&1

# Bing — daily at 04:45 UTC
45 4 * * *  cd $HERMES_HOME && $PYTHON pullers/bing_pull.py >> logs/hermes_bing.log 2>&1
